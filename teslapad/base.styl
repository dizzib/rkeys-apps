@require 'mixin/keys'

K = 23mm

key-size(K)
key-color(black, yellow)
label-color(white, black)

body
  background  #222
  color       white
  font-family arial
  width       K*20
  > div
    float     left
